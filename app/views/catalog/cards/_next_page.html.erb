<%= turbo_frame_tag(
      "pagination",
      loading: :lazy,
      src: more_catalog_cards_path(format: :turbo_stream, page: @next_page)
    ) do %>
  <hr class="col-span-full w-48 h-1 mx-auto my-2 bg-gray-100 border-0 rounded-sm md:my-10 dark:bg-gray-700">
<% end %>